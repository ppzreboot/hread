{
    "type": "module",
    "scripts": {
        "build": "tsc --noEmit && node build.js"
    },
    "dependencies": {
        "@aws-sdk/client-polly": "^3.879.0"
    },
    "devDependencies": {
        "esbuild": "^0.25.9",
        "typescript": "^5.9.2"
    }
}
